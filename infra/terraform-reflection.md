39.1 
    i think the import blocks were most important to prevent unnecessary typing. a lot of the import blocks held values that appeared in many different places of the code, so we didn't have to retype it. this also helps in case any of the values change, we can ensure the value is updated in one spot and it doesn't break the rest of the code or risk indescrepencies. furthermore, they're important here because we've already created the infrastructure in another location, if we were doing this from scratch, then terraform could make that infrastructure for us (hence only wanting imports here-no changes or additions). it ensures connection between terraform and aws
39.2
    i believe we didn't use terraform to manage parameter secrets because it is more secure. i suppose a situation when you would want to use terraform to manage parameter store secrets is when the secrets don't need much security, if any at all, and need to be repeated across many areas of the infrastructure